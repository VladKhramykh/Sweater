<#include "security.ftl"><#import "pager.ftl" as p><@p.pager url page/><div class="card-columns">    <#list page.content as message>        <div class="card my-3" style="width: 22rem;">            <#if message.filename??>                <img class="card-img-top" alt="Card image cap" src="/img/${message.filename}">            </#if>            <div class="card-body">                <h5 class="card-title">#${message.tag}</h5>                <p class="card-text">${message.text}</p>            </div>            <div class="card-footer text-muted container">                <div class="row">                    <a class="col align-self-center" href="/user-messages/${message.author.id}">${message.authorName}</a>                    <#if message.meLiked>                        <a class="col align-self-center" href="/messages/${message.id}/dislike">                            <i class="fa fa-heart" aria-hidden="true"></i>                        </a>                    <#else>                        <a class="col align-self-center" href="/messages/${message.id}/like">                            <i class="fa fa-heart-o" aria-hidden="true"></i>                        </a>                    </#if>                    ${message.likes}                    <#if currentUserId == message.author.id>                        <a class="col btn btn-light" href="/user-messages/${message.author.id}?message=${message.id}">Edit</a>                    </#if>                </div>            </div>        </div>    <#else>        No message    </#list></div>