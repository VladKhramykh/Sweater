<#include "security.ftl"><#import "pager.ftl" as p><div class="card-columns">    <#list page.content as message>        <div class="card my-3">            <#if message.filename??>                <img class="card-img-top" alt="Card image cap" src="/img/${message.filename}">            </#if>            <div class="card-body">                <h5 class="card-title">#${message.tag}</h5>                <p class="card-text">${message.text}</p>            </div>            <div class="card-footer text-muted container">                <div class="row">                    <a class="col align-self-center" href="/user-messages/${message.author_id}">${message.author_name}</a>                    <div class="col align-self-center">                        <#if message.meLiked>                            <a href="/messages/${message.id}/dislike">                                <i class="fa fa-heart like" aria-hidden="true"></i>                            </a>                        <#else>                            <a href="/messages/${message.id}/like" class="dislike">                                <i class="fa fa-heart-o" aria-hidden="true"></i>                            </a>                        </#if>                        ${message.likes}                    </div>                    <#if currentUserId == message.author_id>                        <a class="col link-a-s" href="/user-messages/${message.author_id}?message=${message.id}">Edit</a>                    </#if>                    <#if isAdmin>                        <a class="delete" href="/delete-message/${message.id}">                            <i class="col fas fa-times delete" aria-hidden="true"></i>                        </a>                    </#if>                </div>            </div>        </div>    <#else>        No message    </#list></div><@p.pager url page/>